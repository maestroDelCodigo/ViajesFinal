import { ViajeEstadoPipe } from './viaje-estado.pipe';

describe('ViajeEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new ViajeEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
